#pragma once

#include <stdint.h>

// Timing probes generated by scripts/gen_asm.py (probes_thumb2.S, probes.c).
extern const uint8_t PROBE_COUNT;
uint32_t probe_insns(unsigned id);
void     run_probe(unsigned id, uint32_t iterations, uint32_t *buf);
